"""UK Founder Radar."""

# The single copy of the package version: pyproject.toml reads it at build
# time (dynamic version) and the CLI's `--version` reads it at run time.
# Unrelated to SCORER_VERSION, which versions the scoring model, not the
# package.
__version__ = "2.0.0"
